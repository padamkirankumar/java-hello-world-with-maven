pipeline {
    agent {
        label 'tomcatvm'
    }
    tools {
        maven 'mvn396'
        jdk 'jdk11'
    }
    stages {
        stage('Build') {
            steps {
                sh 'java -version'
                sh 'mvn --version'
            }
        }
    }
}
